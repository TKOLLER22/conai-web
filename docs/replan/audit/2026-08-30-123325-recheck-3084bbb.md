---
skill: recheck
version: 1.3.1
timestamp: 2026-08-30T10:33:25Z
model: sonnet
plan_hash: 3084bbb
plan_ref:
  kind: path
  value: ~/.claude/plans/transient-squishing-galaxy.md
security_guidance:
  detected: false
  reason: none
autonomous_mode: false
lessons_injected_bytes: 0
lessons_truncated: false
code_review_pass:
  ran: false
  reason_skipped: no-working-diff
---

## Plan summary (first 200 chars)
ConAI Website Rebuild — full site (SK+EN) in Next.js/React/Tailwind with GSAP animations and a 3D hero from the client's logo GLB, keeping the existing color world but bolder. Deploys to Coolify VPS.

## Agents dispatched
| Agent | Verdict | Critical | Important | Minor |
|---|---|---|---|---|
| Plan Compliance | ISSUES FOUND | 3 | 4 | 3 |
| Code Quality | ISSUES FOUND | 0 | 5 | 5 |
| Security | PASS | 0 | 0 | 3 |
| Test Coverage | ISSUES FOUND | 1 | 2 | 2 |
| Performance | CONCERNS | 1 | 2 | 3 |
| Fresh Perspective | ISSUES FOUND | 3 | 4 | 5 |
| Project Standards | ISSUES FOUND | 0 | 4 | 4 |

## Per-agent findings (deduplicated synthesis)

### Critical (after dedup)
- components/ui/Section.tsx:37: Container still max-w-6xl (72rem) while Header/Hero/Ladder pinned CSS assume 88rem — every inner page ~256px narrower than intended; the round-1 widening edit silently failed to match the template literal. (Standards + Plan Compliance)
- app/: no error.tsx anywhere + Hero3D useGLTF unwrapped — r3f Canvas re-throws GLTF/WebGL failures, so a failed model fetch or WebGL init crashes the whole homepage. Plan §4 required a WebGL-failure fallback. (Fresh + Quality + Plan)
- components/home/Hero.tsx:17: show3D gates only on viewport width — prefers-reduced-motion users get the fully animating 3D icon; plan §4 required a poster/static fallback. (Plan Compliance)
- lib/seo.ts:4: seoAlternates("/") emits canonical/hreflang "/sk/" (trailing slash) while the page serves at "/sk" — homepage canonical disagrees with its own URL. (Test Coverage)
- components/layout/LocaleSwitcher.tsx:16 + locale-specific article slugs: switching locale on any insights article hard-404s (slug missing in target locale). User-sanctioned tradeoff, but a fallback to /insights is cheap. (Fresh Perspective)

### Important (after dedup)
- Hero intro flash-then-replay on slow hydration: data-hero-fade elements have no pre-hydration CSS hiding (unlike Reveal); content paints, then snaps hidden and replays. Fix: CSS-hide fade elements (NOT the LCP H1) + noscript override. (Fresh)
- npm run lint fails: react-hooks/set-state-in-effect at Hero.tsx:19 and Header.tsx:41. (Plan §9)
- app/[locale]/layout.tsx:14-32: Bricolage/Inter load full variable axes; only weights 600/700 (Bricolage) and 400/500 (Inter) are used — pin weights to trim LCP-critical font bytes. (Performance)
- components/home/Ladder.tsx:67: pin distance measured before font swap; no document.fonts.ready → ScrollTrigger.refresh(). (Fresh)
- components/roi/Calculator.tsx:34: focus:outline-none strips the global focus ring on all 8 inputs; also label htmlFor targets the range input, not the number input. (Standards + Quality)
- components/layout/Header.tsx:72,133,146: hardcoded English aria-labels bypass next-intl. (Standards)
- lib/insights.ts:27-37: frontmatter cast without validation → Invalid Date / "undefined | ConAI" ships silently on a malformed future article. (Test + Quality)
- app/[locale]/advisor + referral pages duplicate PageHeader verbatim to add a badge → add badge prop. (Quality + Standards)
- ROI math untested: extract computeRoi() to lib, add minimal vitest (roi-math, insights frontmatter, seoAlternates regression). Field onChange "Number(x) || min" zero-coercion footgun. (Test)
- Hero3D: no explicit tab-hidden pause (plan §4). Browsers stop rAF in hidden tabs so real impact is minimal — implement cheap visibilitychange gate or record as accepted deviation. (Plan)

### Minor (after dedup)
- Dead code: .marquee/.marquee-track + hairline-b CSS in globals.css; unused common.nav.advisor message key.
- components/seo/JsonLd.tsx:5: escape "<" as < in JSON.stringify output (hardening).
- app/[locale]/opengraph-image.tsx: add hasLocale guard for consistency (defense-in-depth).
- next.config.ts: no security headers — consider baseline CSP/nosniff scoped for cal.eu + fonts.
- Dockerfile:27: public/ COPY missing --chown=nextjs:nodejs (inconsistent ownership).
- lib/seo.ts:1: SITE_URL hardcoded; staging deploys emit prod canonicals (env override).
- GLB 160KB vs plan ≤150KB budget.
- lucide strokeWidth 1.5 only on Ladder arrows; 2px default elsewhere (anti-slop consistency).
- Header desktop/mobile nav arrays maintained separately (drift risk).
- FinalCta lives in components/home but used by 9 routes; insights date formatter duplicated; not-found.tsx hand-rolls Button gradient.
- SmoothScroll effects:true unused; Hero3D reads raw window.scrollY (desynced from smoothed transform — masked by lerp).
- Pulse badge dot missing aria-hidden; ROI ramp model slightly optimistic + 36-month cap shows "—"; Field id derived from label text (collision risk); backdrop-blur-xl on header worth profiling on mid-tier Android.

## Plan compliance summary
- ~46/52 discrete plan items fully implemented; all 15 routes ×2 locales, SEO layer, content parity, Docker verified by agents.
- Gaps concentrated in plan §4 (3D pipeline): reduced-motion/WebGL fallback missing, tab-hidden pause missing, frameloop always-vs-demand deviation, idle-mount deferral partial.
- Deviations: sanctioned list executed cleanly (Next 16, no work filter, homepage redesign, folded EN pass, locale-specific slugs, privacy Cal.com wording, nomotion hatch — the latter judged justified scope, not creep).
- Scope creep: ROI "monthly budget" input (justified — required outputs impossible without it); amber badge on referral (third amber context, minor).
- Verification gap: lint failing despite milestone-9 "polish" claim; mobile 390px pass + deployment still open as disclosed.

## Outcome
- fixes_offered: 33
- fixes_applied: 31 (all 5 critical, all 10 important, 16 minor; verified via lint + tsc + 12 vitest tests + build + prod smoke + Lighthouse re-run: mobile 90, TBT 0ms, CLS 0)
- fixes_resolved_as_accepted: 2 — GLB stays 160KB vs ≤150KB target (optimize floor without lossy simplification); font weight-pinning applied then REVERTED with measurement evidence (static instances scored worse than the variable font: 87 vs 90)
- deviations_finalized: frameloop "always-while-visible" kept over plan's "demand" (continuous idle rotation requires it; offscreen/tab-hidden pause implemented); plan's poster fallback replaced by graceful degradation (reduced-motion/no-WebGL/load-failure render the aura backdrop only — no poster asset shippable without a WebGL render environment)
- open_items: mobile 390px visual pass (blocked by local Chrome/macOS Local Network permission); services "4 tiers + addon + retainer" vs plan's literal "5 tiers" wording awaiting user gut-check; deployment (milestone 10) pending git remote + domain
- lesson_captured: yes (interactive)
- lesson_topic: scripted string-replacement edits must assert their match
