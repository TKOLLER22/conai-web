---
skill: recheck
version: 1.3.1
timestamp: 2026-08-30T14:16:03Z
model: sonnet
plan_hash: 05c4ee7
plan_ref:
  kind: conversation
  value: post-launch-prep delta feature set (7 items), commits fb9ec96..8ca79bb
security_guidance:
  detected: false
  reason: none
autonomous_mode: false
lessons_injected_bytes: 863
lessons_truncated: false
code_review_pass:
  ran: false
  reason_skipped: no-working-diff
---

## Plan summary (first 200 chars)
Delta plan: prefix-less locale URLs (cookie-based), staging-safe deploys, route-change scroll fix, light/dark mode, mobile menu containing-block fix, EN hero + work grid visual fixes, standing constraints.

## Agents dispatched
| Agent | Verdict | Critical | Important | Minor |
|---|---|---|---|---|
| Plan Compliance | ISSUES FOUND | 2 | 2 | 2 |
| Code Quality | ISSUES FOUND | 2 | 3 | 3 |
| Security | PASS | 0 | 3 | 3 |
| Test Coverage | ISSUES FOUND | 0 | 3 | 2 |
| Fresh Perspective | ISSUES FOUND | 1 | 4 | 5 |
| Project Standards | ISSUES FOUND | 2 | 0 | 2 |

## Per-agent findings (deduplicated synthesis)

### Critical (after dedup)
- proxy.ts:11 + all SSG documents: Vary: Cookie never reaches page responses (only middleware redirects); documents ship Cache-Control: s-maxage=31536000 while content varies by NEXT_LOCALE cookie — any shared cache (future CDN/proxy) would serve one visitor's language to everyone for up to a year. Verified in a real docker run by Fresh Perspective + live curl by Plan Compliance. Root cause: middleware header appends don't survive Next's page-serving layer on rewrites.
- components/layout/LocaleSwitcher.tsx:20: npm run lint FAILS at HEAD — react-hooks/immutability on the document.cookie assignment (introduced in 8ca79bb; the pre-commit lint check piped through tail swallowed the exit code). Plus @next/next/no-location-assign-relative-destination warning on the /insights fallback (deliberate but unsuppressed/unjustified).
- Dockerfile:16-19 + README: staging-default build arg has the WRONG failure direction — following the README verbatim at conai.sk launch ships robots Disallow:/, noindex meta and staging canonicals to production (silent SEO blackout). Staging protection should not depend on a build arg at all (request-time host check), and the ARG default should be the production URL.
- components/layout/LocaleSwitcher.tsx:25 (router.refresh): Next router cache keeps other routes' prefetched RSC payloads under the OLD locale (~5 min staleTimes) — navigating right after a language switch renders mixed-locale pages. Only /insights was special-cased.

### Important (after dedup)
- components/layout/Header.tsx:66-75: backdrop-filter remains in the transition set — during the 300ms open/close transition while scrolled, the interpolating filter re-creates the containing block and can clip the fixed overlay mid-animation (steady states verified fine).
- components/ui/Logo.tsx + Header.tsx: both theme logo variants always fetch, and BOTH get priority preload — wasted high-priority request on every load.
- lib/seo.ts: seoAlternates name + dead _locale param no longer honest (returns only canonical) — rename to seoCanonical.
- lib/seo.ts SITE_URL/IS_PRODUCTION_DOMAIN: zero tests pinning env semantics (unset/empty/staging/prod) — the one flag that decides indexing; ||-vs-?? empty-string behavior unpinned.
- app/[locale]/insights/[slug]/page.tsx findInsight: real branching logic, untestable in page module — move to lib/insights.ts + test (own locale wins / cross-locale fallback / null).
- LocaleSwitcher cookie missing Secure attribute; SmoothScroll document.querySelector(hash) can throw on malformed hash (no try/catch).
- Dockerfile HEALTHCHECK targets /sk (works via 307 follow, fragile) — point at /.
- package.json: puppeteer-core devDependency referenced nowhere in the tree — wire to a committed script or remove.
- README stale: "(redirects to /sk)" now false; Coolify section contradicts the staging-default strategy.

### Minor (after dedup)
- Vary: Cookie varies on the whole cookie header — future CDN cache fragmentation note.
- app/globals.css: two body::after rules 115 lines apart, override works by specificity not order — co-locate.
- ThemeToggle icon can flash Sun→Moon one frame post-hydration (server snapshot "dark"); page colors correct via boot script.
- No-JS visitors always get dark theme (no CSS prefers-color-scheme fallback) — acceptable, dark is default.
- Back/Forward navigation lands at top (scroll restoration not distinguishable from push in the pathname effect) — pre-existing ScrollSmoother+router tradeoff.

## Plan compliance summary
- 6/7 plan items fully implemented and mostly verified empirically (live curls, docker run, byte-level checks); item 1 partial (Vary requirement not actually effective on document responses), item 7 failing (lint red at HEAD).
- Deviations: none unapproved.
- Scope creep: puppeteer-core devDependency (unwired); token-sweep of white/* literals (justified — required for light mode).

<!-- Outcome appended in Step 8 -->
